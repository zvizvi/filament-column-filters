includes:
    - phpstan-baseline.neon

parameters:
    level: 4
    paths:
        - src
    tmpDir: build/phpstan
    ignoreErrors:
        # Macro closures are rebound to the column instance at runtime.
        -
            identifier: variable.undefined
            path: src/FilamentColumnFilters.php
        -
            identifier: varTag.variableNotFound
            path: src/FilamentColumnFilters.php
